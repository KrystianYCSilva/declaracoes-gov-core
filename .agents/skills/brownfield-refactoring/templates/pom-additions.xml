<!-- Add these dependencies inside <dependencies> in pom.xml -->

<!-- Test: H2 in-memory database (DB2 compatibility mode) -->
<dependency>
    <groupId>com.h2database</groupId>
    <artifactId>h2</artifactId>
    <version>2.2.224</version>
    <scope>test</scope>
</dependency>

<!-- Test: JUnit 5 -->
<dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter</artifactId>
    <version>5.12.2</version>
    <scope>test</scope>
</dependency>

<!-- Test: Mockito -->
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-core</artifactId>
    <version>4.11.0</version>
    <scope>test</scope>
</dependency>

<!-- If project uses JUnit 4 tests, add vintage engine for backward compatibility -->
<dependency>
    <groupId>org.junit.vintage</groupId>
    <artifactId>junit-vintage-engine</artifactId>
    <version>5.12.2</version>
    <scope>test</scope>
</dependency>

<!-- JAXB (only if mvn compile fails with "javax.xml.bind not found") -->
<!--
<dependency>
    <groupId>javax.xml.bind</groupId>
    <artifactId>jaxb-api</artifactId>
    <version>2.3.1</version>
</dependency>
<dependency>
    <groupId>org.glassfish.jaxb</groupId>
    <artifactId>jaxb-runtime</artifactId>
    <version>2.3.9</version>
</dependency>
<dependency>
    <groupId>javax.annotation</groupId>
    <artifactId>javax.annotation-api</artifactId>
    <version>1.3.2</version>
</dependency>
-->


<!-- Add these plugins inside <build><plugins> in pom.xml -->

<!-- Surefire: must forward JaCoCo agent via ${argLine} -->
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-surefire-plugin</artifactId>
    <version>3.2.5</version>
    <configuration>
        <argLine>${argLine}</argLine>
    </configuration>
</plugin>

<!-- JaCoCo: coverage measurement + 70% enforcement -->
<plugin>
    <groupId>org.jacoco</groupId>
    <artifactId>jacoco-maven-plugin</artifactId>
    <version>0.8.12</version>
    <executions>
        <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
        </execution>
        <execution>
            <id>report</id>
            <phase>test</phase>
            <goals><goal>report</goal></goals>
        </execution>
        <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
                <rules>
                    <rule>
                        <element>BUNDLE</element>
                        <limits>
                            <limit>
                                <counter>LINE</counter>
                                <value>COVEREDRATIO</value>
                                <minimum>0.70</minimum>
                            </limit>
                        </limits>
                    </rule>
                </rules>
            </configuration>
        </execution>
    </executions>
    <configuration>
        <excludes>
            <!-- Controllers (need ZK/Servlet container) -->
            <exclude>**/controlador/**</exclude>
            <exclude>**/controller/**</exclude>
            
            <!-- ZK UI utilities -->
            <exclude>**/Sessao.class</exclude>
            <exclude>**/ZkUtils.class</exclude>
            
            <!-- Database connection (native driver) -->
            <exclude>**/ConexaoDB2.class</exclude>
            
            <!-- Auth/LDAP -->
            <exclude>**/LdapUtil.class</exclude>
            
            <!-- Email -->
            <exclude>**/Email.class</exclude>
            
            <!-- ADD PROJECT-SPECIFIC EXCLUSIONS BELOW -->
            
        </excludes>
    </configuration>
</plugin>

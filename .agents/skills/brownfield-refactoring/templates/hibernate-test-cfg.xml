<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<!--
  H2 in-memory database configuration for integration tests.
  
  INSTRUCTIONS:
  1. Replace {SCHEMA} with the schema used in @Table annotations.
     Find it: grep -rn "@Table" src/java --include="*.java" | grep schema
     Examples: ASP, GPE, EST, PUBLIC
     If no schema found, remove the INIT parameter entirely.
     
  2. Add ALL entity classes as <mapping class="..."/> entries.
     Find them: grep -rn "@Entity" src/java --include="*.java" -l
     
  3. Place this file at: src/test/resources/hibernate-test.cfg.xml
-->
<hibernate-configuration>
  <session-factory>
    <property name="hibernate.connection.driver_class">org.h2.Driver</property>
    <property name="hibernate.connection.url">jdbc:h2:mem:test;MODE=DB2;INIT=CREATE SCHEMA IF NOT EXISTS {SCHEMA}</property>
    <property name="hibernate.connection.username">sa</property>
    <property name="hibernate.connection.password"/>
    <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>
    <property name="hibernate.hbm2ddl.auto">create-drop</property>
    <property name="hibernate.show_sql">false</property>
    <property name="hibernate.format_sql">false</property>

    <!-- Map ALL @Entity classes below. One per line. -->
    <!-- <mapping class="com.example.Entity1"/> -->
    <!-- <mapping class="com.example.Entity2"/> -->
  </session-factory>
</hibernate-configuration>
